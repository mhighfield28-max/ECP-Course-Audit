version: 1
frontend:
  phases:
    build:
      commands:
        - sed -i "s|__PROXY_URL__|$PROXY_URL|g" index.html
  artifacts:
    baseDirectory: .
    files:
      - index.html
  cache:
    paths: []
